import 'package:flutter/material.dart';

class CustomChekBoxOptional extends StatelessWidget {
  const CustomChekBoxOptional({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
